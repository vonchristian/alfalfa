<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar bar1"></span>
        <span class="icon-bar bar2"></span>
        <span class="icon-bar bar3"></span>
      </button>
      <a class="navbar-brand title" href="">
        <%="Expenses" if current_page?(accounting_expenses_path) %>
        <%="Transactions" if current_page?(accounting_entries_path) %>
      </a>
    </div>
    <div class="nav navbar-nav navbar-left">
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">
            <%=image_tag(current_user.profile_photo.url(:small), class: "img-circle", height: 25, width: 25) %>
            <p><%=current_user.first_name %>
              <small> (<%=current_user.role.titleize %>)</small>
            </p>
            <b class="caret"></b>
          </a>
          <ul class="dropdown-menu">
            <li><%=link_to "Profile", user_path(current_user) %></li>
            <li><%=link_to "Sign out", destroy_user_session_path, method: :delete %></li>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>
