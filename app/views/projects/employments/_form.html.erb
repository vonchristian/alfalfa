<%= simple_form_for ([@project, @employment]) do |f| %>
  <%=f.error_notification %>
  <%=f.input :employee_id, collection: Employee.all, label: false, placeholder: 'Employee' %>
    <div class="modal-footer">
      <%=f.submit "Add this Employee", class: "btn btn-primary" %>
      <%=link_to "or I'm done adding Employees",project_path(@project), class: "btn"%>
    </div>
<% end %>
<script>
    $(document).ready(function() {
        $("#employment_employee_id").select2({
            theme: "bootstrap"
        });
    });
</script>