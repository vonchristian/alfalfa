$("ul.errors").html("")
<% if @bid_expense.errors.any? %>
  <% @bid_expense.errors.full_messages.each do |message| %>
    $("ul.errors").append($("<li />").html("<%= message.html_safe %>"))
  <% end %>
<% else %>
  $("#bid-expense-modal").modal("hide")
  window.location.reload();
<% end %>
