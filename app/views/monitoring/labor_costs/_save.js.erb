$("ul.errors").html("")
<% if @labor_cost.errors.any? %>
  <% @labor_cost.errors.full_messages.each do |message| %>
    $("ul.errors").append($("<li />").html("<%= message.html_safe %>"))
  <% end %>
<% else %>
  $("#labor-cost-modal").modal("hide")
  window.location.reload();
<% end %>
