<div class="col-md-12">
  <div class="box box-solid">
    <div class="box-header with-border">
      <h3 class="widget-user-username">List of Issued Inventories</h3>
    </div>
    <div class="box-body">
      <div class="col-md-8 border-right">
        <table class="table table-compressed table-hover">
          <thead>
            <tr>
              <th style="width: 20%">Issued To</th>
              <th style="width: 15%">Material</th>
              <th>Quantity</th>
              <th>Date Issued</th>
              <th><span class="pull-right">Unit Cost</span></th>
              <th><span class="pull-right">Total Cost</span></th>
            </tr>
          </thead>
          <tbody>
            <% if @issued_inventories.present? %>
              <% @issued_inventories.each do |issued_inventory| %>
                <tr>
                  <td><%= issued_inventory.contractor.try(:name) %></td>
                  <td><%= issued_inventory.inventory.try(:name) %></td>
                  <td><%= issued_inventory.quantity %></td>
                  <td><%=issued_inventory.date_issued.strftime("%B %e, %Y") %></td>
                  <td><span class="pull-right"><%= number_to_currency issued_inventory.unit_cost %></span></td>
                  <td><span class="pull-right"><%= number_to_currency issued_inventory.try(:total_cost) %></span></td>
                </tr>
              <% end %>
            <% end %>
          </tbody>
        </table>
      </div>
      <div class="col-md-4">
        <%= render 'form' %>
      </div>
    </div>
  </div>
</div>
