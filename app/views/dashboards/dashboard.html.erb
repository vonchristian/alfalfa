<div class="row">
<div class="col-md-4">
<div class="box box-primary">
projects
      <span class="pull-right"><%=link_to new_project_path, class: "btn btn-default bg-blue-active pull-right" do %><i class="fa fa-user-plus"></i> New project <%end%></span>
    </h4>
  
            <% @projects.each do |project| %>
             <div class="row">
            <div class="col-md-1">
            <%#= image_tag attachment_url(project, :profile_image, :fill, 60, 60, format: "jpg", fallback: "default-small.png"),  class: "img-circle" %>
            </div>
            <div class="col-md-7 col-sm-9 col-xs-9">
            <p><%= link_to project.name.upcase, project %></p>
            <span class="pull-right"><%=number_to_currency project.cost %> </span>
            <span class="help-block"><i class="fa fa-calendar"></i><%=project.duration %></span>
            </div>
            </div>
           <hr />
            <% end %>
       </div>
 
