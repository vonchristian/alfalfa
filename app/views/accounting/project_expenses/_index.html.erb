<div class="tab-pane active" id="project">
  <br>
  <h4 class="title">Project
    <%=link_to "New Expense", new_accounting_project_expense_path, class: "btn btn-danger pull-right" %>
  </h4>
  <table class="table table-hover">
  	<thead>
			<tr>
				<th></th>
				<th> Description </th>
				<th> Employee </th>
				<th> Credit </th>
				<th> Debit </th>
				<th> Amount</th>
				<th> Type </th>
			</tr>
		</thead>
		<tbody>
			<% @project_expenses.each do |project_expense| %>
				<tr>
					<td>

					</td>
          <td>
            <small class="help-block pull-right"><i class="fa fa-clock-o"></i> <%=project_expense.created_at.strftime("%B %d, %Y %I:%M%p") %></small><br>
            <small><%=link_to project_expense.description.truncate(200), accounting_project_expense_path(project_expense) %></small>
          </td>

					<td>
            <small><%=project_expense.entriable.try(:name) %></small>
          </td>
					<td>
						<% project_expense.credit_amounts.each do |credit| %>
							<small><%=credit.account.name %></small>
						<% end %>
					</td>
					<td>
						<% project_expense.debit_amounts.each do |debit| %>
						  <small><%=debit.account.name %></small><br>
						<% end %>
					</td>
					<td>
						<% project_expense.credit_amounts.each do |credit| %>
							<small><%=number_to_currency credit.amount %></small>
						<% end %>
					</td>
					<td><small><%#=project_expense.type.sub("Transactions::", "").titleize if project_expense.type %></small></td>
				</tr>
			<% end %>
		</tbody>
  </table>
</div>