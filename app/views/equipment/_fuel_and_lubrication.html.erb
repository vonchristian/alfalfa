<div class="card">
  <div class="header">
    <h4 class="title">Fuel and Lubrication
      <% if @line_items.present? %>
        <%=link_to equipment_orders_path do %>
          <span class="btn btn-info pull-right" title="View PDF Report"><i class="fa fa-print"></i>Generate Report</span>
        <% end %>
      <% else %>
        <%=link_to nil do %>
          <span class="btn disabled pull-right" data-toggle="tooltip" data-placement="bottom" title="View PDF Report"><i class="fa fa-print"></i>Generate Report</span>
        <% end %>
      <% end %>
    </h4>
    <div class="row"><hr></div>
  </div>
  <div class="content">
    <table class="table table-hover">
      <thead>
        <tr class="text-uppercase">
          <th>Date Issued</th>
          <th>Quantity</th>
          <th>Total Cost</th>
        </tr>
      </thead>
      <tbody>
        <% @line_items.each do |line_item| %>
          <tr>
            <td><%=line_item.date.strftime("%B %e, %Y") %></td>
            <td><%=line_item.quantity %> <%=line_item.inventory.unit %></td>
            <td><%=number_to_currency line_item.total_cost %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
    <center><%= link_to 'View All', equipment_line_items_path(@equipment) %></center>
    <div class="modal-footer">
      <center><%= paginate @line_items, :window => 1 %></center>
    </div>
  </div>
</div>
